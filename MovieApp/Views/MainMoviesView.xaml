<UserControl x:Class="MovieApp.Views.MainMoviesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MovieApp.Views">
    <UserControl.Resources>
        <local:FavoriteButtonTextMultiConverter x:Key="FavoriteButtonTextMultiConverter"/>
        <local:FavoriteButtonVisibilityMultiConverter x:Key="FavoriteButtonVisibilityMultiConverter"/>
        <local:NotFavoriteButtonVisibilityMultiConverter x:Key="NotFavoriteButtonVisibilityMultiConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="Название" CommandParameter="Title" Command="{Binding ChangeSortCommand}" Margin="0,0,5,0"/>
            <Button Content="Год" CommandParameter="Year" Command="{Binding ChangeSortCommand}" Margin="0,0,5,0"/>
            <Button Content="Жанр" CommandParameter="Genre" Command="{Binding ChangeSortCommand}" Margin="0,0,5,0"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Movies}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5" Background="White">
                            <Border.InputBindings>
                                <MouseBinding Gesture="LeftClick"
                                              Command="{Binding DataContext.OpenMovieDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              CommandParameter="{Binding}" />
                            </Border.InputBindings>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding PosterUrl}" Width="100" Height="150" Margin="0,0,10,0"/>
                                <StackPanel Grid.Column="1" Margin="0">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Year}" FontSize="14"/>
                                    <TextBlock Text="{Binding Genres[0]}" FontSize="12"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                        <Button
                                            Command="{Binding DataContext.AddToFavoritesCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Width="140"
                                            HorizontalContentAlignment="Center">
                                            <Button.Content>
                                                <MultiBinding Converter="{StaticResource FavoriteButtonTextMultiConverter}">
                                                    <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                    <Binding/>
                                                </MultiBinding>
                                            </Button.Content>
                                            <Button.Visibility>
                                                <MultiBinding Converter="{StaticResource FavoriteButtonVisibilityMultiConverter}">
                                                    <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                    <Binding/>
                                                </MultiBinding>
                                            </Button.Visibility>
                                        </Button>
                                        <Button Content="Удалить из избранного"
                                            Command="{Binding DataContext.RemoveFromFavoritesCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Width="140"
                                            HorizontalContentAlignment="Center">
                                            <Button.Visibility>
                                                <MultiBinding Converter="{StaticResource NotFavoriteButtonVisibilityMultiConverter}">
                                                    <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                                    <Binding/>
                                                </MultiBinding>
                                            </Button.Visibility>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,10">
            <Button Content="Назад" Command="{Binding PrevPageCommand}" Margin="5"/>
            <TextBlock Text="{Binding CurrentPage}" FontWeight="Bold" FontSize="16" Margin="10,0"/>
            <TextBlock Text="/" FontSize="16"/>
            <TextBlock Text="{Binding TotalPages}" FontSize="16" Margin="10,0"/>
            <Button Content="Вперёд" Command="{Binding NextPageCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl> 